[package]
name = "governance"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
soroban-sdk = "20.0.0"
treasury = { path = "../treasury" }

[dev-dependencies]
soroban-sdk = { version = "20.0.0", features = ["testutils"] }
